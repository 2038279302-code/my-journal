# 变更记录

本文件记录项目的所有功能变更、问题修复和优化内容，按时间倒序排列。

---

## [0.6.0] - 2026-06-05

### 新增
- **💡 灵感捕捉**：阅读日报时，悬停资讯卡片右上角出现「💡 记灵感」按钮；点击弹出轻量浮层，自动预填「来源板块 · 资讯标题」上下文；支持 `⌘↵` 快速保存为碎片想法，`Esc` 关闭；内容带来源前缀，在笔记中一眼看清灵感来源
- **📝 今日回顾**：每篇日报详情页底部新增「今日回顾」区域（紫色调）；未写时显示引导 + 写入入口，写后展示内容 + 时间戳 + 编辑按钮；数据存入 `notes` 表（type=reflection），通过隐形 `[brief:日期]` marker 与日报绑定
- **GET /api/notes 新增 `content_contains` 参数**：支持通过内容关键字精确查询笔记，用于快速定位当日日报回顾

### 优化
- **反思复盘页**：自动识别日报回顾笔记，在卡片头部展示「📰 2026-XX-XX 日报回顾」来源标签；正文展示时自动过滤隐形 marker，无技术痕迹
- **记录详情页**：同样识别日报回顾来源，顶部显示「📰 来自 XXXX-XX-XX 日报的今日回顾」溯源标签；正文、字数统计均去除 marker 干扰

---

## [0.5.0] - 2026-06-05

### 新增
- **记录详情页** `/notes/[id]`：完整展示单条记录内容（自动识别 Markdown 渲染）、标签、情绪、类型、创建/更新时间、字数统计；从首页卡片、搜索结果、复盘列表均可进入
- **详情页操作**：提供返回、编辑（复用 NoteEditor 弹窗）、删除按钮；删除前二次确认弹窗，避免误删
- **GET /api/notes/:id**：新增单条记录查询接口，404 时返回友好错误
- **Toast 全局提示系统**：首页、复盘页、详情页所有操作（保存成功/失败、删除成功/失败）均有温暖的浮动提示文案

### 优化
- **搜索能力全面升级**：`/search` 页新增类型筛选（全部/碎片/日记/复盘）、时间范围筛选（近 7 天/近 30 天/近 3 个月/自定义起止日期）；搜索结果点击跳转详情页；空态/无结果/网络错误均有友好提示
- **搜索 API 升级**：`/api/search` 支持 `type`、`date_from`、`date_to` 参数；同时搜索正文 ilike 和标签 contains，合并去重后返回
- **标签计数修正**：新建时每个唯一标签 count+1（去重，避免同篇重复计数）；编辑时对比新旧标签差量更新；删除时对应标签 count-1，归零自动删除标签记录
- **热力图文案统一**：WEEKS=26，文案改为「近半年记录 N 次」（原为「今年共记录」），统计范围与组件展示范围保持一致
- **复盘页删除交互**：改为二次确认弹窗 + Toast，而非直接删除
- **复盘页内容点击**：点击记录卡片内容区域或「查看全文」可跳转详情页
- **首页删除同步**：删除记录后前端同步更新热力图数据和标签计数显示

### 移动端适配
- **响应式导航**：≤768px 隐藏左侧边栏，改为顶部固定栏（高度 56px）+ 汉堡菜单抽屉
- **新增 MainContent 组件**：客户端动态控制 marginLeft / paddingTop，避免 CSS media query 在 Next.js SSR 中失效
- **热力图**：容器加 `overflow-x: auto` 支持移动端横向滑动
- **按钮点击区域**：移动端 `.btn-journal` min-height 提升到 40px

---

## [0.4.0] - 2026-06-05

### 新增
- **AI 日报一键生成**：在网站内直接触发，无需借助外部工具
  - 新增 `/api/generate-brief` SSE 流式接口
  - 并行抓取 8 个信息源（follow-builders X/Blogs feed、OpenAI、Anthropic、36氪、量子位、机器之心、Hacker News）
  - 调用 OpenAI 兼容 AI 接口（支持 MiniMax / Claude / GPT / DeepSeek 等）提炼生成结构化日报
  - 左侧实时进度日志，右侧打字机流式预览 AI 生成过程
  - 生成完成后自动保存至数据库，左侧列表自动刷新

### 优化
- AI 日报页面交互全面改版：移除手动同步按钮，改为一键生成入口
- 空状态引导优化，支持中止生成

### 配置
- 新增环境变量：`AI_API_KEY` / `AI_API_BASE_URL` / `AI_MODEL`（支持任意 OpenAI 兼容接口）
- 在 Vercel 生产环境同步配置上述三个环境变量

---

## [0.3.0] - 2026-06-05

### 新增
- 配置 GitHub 仓库并连接 Vercel 自动部署（`git push` → 自动上线，约 40 秒）

### 修复
- 修复 git 作者邮箱为本地主机名导致 Vercel 部署被拦截的问题

### 优化
- 完善 README.md，增加架构图、项目结构、部署说明
- 新增 CHANGELOG.md 变更记录文件

---

## [0.2.0] - 2026-06-04

### 修复
- **关键修复**：补全 Supabase 数据库 RLS（行级安全）策略，解决"数据死活存不进去"的问题
  - 根因：`CREATE POLICY` 和 `GRANT` 语句在建表时未成功执行
  - 修复：在 SQL Editor 中手动执行 GRANT 和 9 条 RLS 策略
- 修复 API 路由 Token 刷新后 Cookie 无法写回浏览器的问题（新增 `createRouteClient`）
- 修复 Next.js 16 中间件命名规范问题（`middleware.ts` → `proxy.ts`）
- 修复根路由文件冲突导致的页面白屏

### 优化
- 将中间件中的 `getUser()`（远程验证）改为 `getSession()`（本地读取），首屏加载提速约 1s
- 配置 Supabase Auth Site URL 为线上域名，修复登录回调跳转问题

---

## [0.1.0] - 2026-06-04

### 新增（初始版本）
- 用户登录/注册（邮箱密码，Supabase Auth）
- **想法记录**：快速记录想法，支持 `#标签` 自动提取，热力图展示全年活跃度
- **AI 日报**：展示每日 AI 领域动态
- **反思复盘**：长文日记入口，支持 Markdown 编辑
- **全文搜索**：基于 PostgreSQL 全文搜索
- 响应式布局，手机端适配
- 部署到 Vercel，线上可访问
